import { C } from "../types/ast";
import { State } from "../types/state";
import { StyProg } from "../types/style";
import { Assignment, DepGraph, Translation } from "../types/styleSemantics";
interface Trio {
    sub: string;
    dsl: string;
    sty: string;
}
export declare const loadProgs: ({ dsl, sub, sty, }: Trio) => Promise<{
    translation: Translation;
    assignment: Assignment;
    state: State;
    styleAST: StyProg<C>;
    graph: DepGraph;
}>;
export {};
//# sourceMappingURL=Style.test.d.ts.map